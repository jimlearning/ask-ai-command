[project]
name = "mcp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "dotenv>=0.9.9",
    "pydantic-ai>=0.0.24",
]

[dependency-groups]
[project.scripts]
ai = "ai:main"
